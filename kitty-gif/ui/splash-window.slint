import { Button, VerticalBox, HorizontalBox } from "std-widgets.slint";

export component Splash inherits Rectangle{
    preferred-height: 320px;
    preferred-width: 240px;
    background: #000000;
    vector := Image {
        source: @image-url("assets/Vector.svg");
    }
}